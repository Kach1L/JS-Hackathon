# from .models import Option

# def bulk_insert_countries(options):
#     options_to_create = [
#         Option(name=opt['name'], flag=opt['png'])
#         for opt in options
#     ]
#     Option.objects.bulk_create(options_to_create)

